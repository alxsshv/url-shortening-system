apiVersion: v1
kind: Service
metadata:
  name: original-link-database-exporter
  namespace: dev
spec:
  clusterIP: None
  selector:
    name: postgres
    system: url-shortening-system
    app: original-link-database
    type: database
    role: replica
  ports:
    - name: original-link-database-exporter
      port: 9187
      targetPort: 9187