apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: dev
spec:
  type: NodePort
  selector:
      name: api
      system: url-shortening-system
      type: application
  ports:
    - port: 8085
      targetPort: 8085
      nodePort: 30085
      protocol: TCP