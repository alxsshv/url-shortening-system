apiVersion: v1
kind: Service
metadata:
  name: tempo
  namespace: dev
spec:
  clusterIP: None
  selector:
    name: tempo
    system: url-shortening-system
    type: monitoring
  ports:
    - name: tempo
      port: 3200
      targetPort: 3200
