apiVersion: v1
kind: Service
metadata:
  name: short-link-database-headless
spec:
  clusterIP: None
  selector:
    system: url-shortening-system
    app: short-link-database
  ports:
    - port: 5432
      targetPort: 5432
      name: short-link-database