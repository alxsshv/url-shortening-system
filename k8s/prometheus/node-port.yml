apiVersion: v1
kind: Service
metadata:
  name: prometheus
  namespace: dev
spec:
  type: NodePort
  ports:
    - port: 9090
      targetPort: 9090
      nodePort: 30090
  selector:
    name: prometheus
    system: url-shortening-system
    type: monitoring